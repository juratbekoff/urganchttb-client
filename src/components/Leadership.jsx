const Leadership = ({ ...elem }) => {
    return (
        <div className="flex mt-5 w-full max-md:flex max-md:flex-col max-md:items-center max-md:gap-4">
            <h1>Rahbariyat</h1>
        </div>
    );
};

export default Leadership;